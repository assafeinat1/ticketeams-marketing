# {{ spec.name }}

{{ spec.description }}

## Installation

```bash
pip install -r requirements.txt
```
{% if spec.requires_api_key %}

## Configuration

1. Edit the `.env` file and set your API key:
   ```
   {{ spec.api_key_var_name }}=your_actual_key_here
   ```
2. The `.gitignore` ensures `.env` is never committed to git.
{% endif %}

## Tools

{% for tool in spec.tools %}
### `{{ tool.name }}`

{{ tool.description }}

{% if tool.parameters %}
| Parameter | Type |
|-----------|------|
{% for param_name, param_type in tool.parameters.items() %}
| `{{ param_name }}` | {{ param_type }} |
{% endfor %}
{% endif %}
**Returns:** `{{ tool.return_type }}`

{% endfor %}

## Running

```bash
python server.py
```

### Add to Claude Code `.mcp.json`

```json
{
  "{{ spec.name }}": {
    "command": "python",
    "args": ["{{ skill_path }}/server.py"]
  }
}
```

---

*Generated by Skill Factory on {{ timestamp }}*
